# Core dependencies
yt-dlp>=2024.10,<2026.0

# Cookie extraction for authentication
browser-cookie3>=0.19.0

# Google search dependencies
requests>=2.25.0
beautifulsoup4>=4.9.0
selenium>=4.15.0  # For browser-based search to bypass restrictions

# AI matching dependencies (optional)
sentence-transformers>=2.2.0
numpy>=1.21.0
torch>=2.0.0
transformers>=4.20.0
tokenizers>=0.13.0
scikit-learn>=1.0.0
scipy>=1.8.0
tqdm>=4.60.0

# PyTorch ecosystem (for GPU acceleration)
# Note: PyTorch is included above, but for GPU support install manually:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# or for CPU-only: pip install torch torchvision torchaudio

# Hugging Face ecosystem (for sentence-transformers)
huggingface-hub>=0.10.0
safetensors>=0.3.0

# Utilities
urllib3>=1.26.0
certifi>=2020.0.0
charset-normalizer>=3.0.0
idna>=3.0.0

# File handling
fsspec>=2021.0.0
filelock>=3.0.0

# System dependencies (not pip installable):
# - ffmpeg: Required for audio extraction (install via system package manager)
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS: brew install ffmpeg
#   Windows: Download from https://ffmpeg.org/download.html

